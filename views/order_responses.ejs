<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Responses | Tanta Car Elite</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --tanta-orange: #ff5e00;
            --main-dark: #09090b;
            --card-bg: rgba(23, 23, 26, 0.8);
            --border-color: rgba(255, 255, 255, 0.08);
            --text-muted: #a1a1aa;
            --accent-purple: #6c5ce7;
        }

        body {
            background-color: var(--main-dark);
            background-image: radial-gradient(circle at 0% 100%, rgba(108, 92, 231, 0.05) 0%, transparent 40%);
            color: #fff;
            font-family: 'Cairo', sans-serif;
            margin: 0;
            padding: 25px;
            min-height: 100vh;
        }

        /* Nav Header */
        .nav-header {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--border-color);
            padding: 20px 35px;
            border-radius: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .nav-header h2 { margin: 0; font-size: 22px; font-weight: 800; }
        .nav-header h2 i { color: var(--accent-purple); margin-left: 10px; }

        .back-btn {
            text-decoration: none;
            color: var(--accent-purple);
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: 0.3s;
        }
        .back-btn:hover { transform: translateX(5px); filter: brightness(1.2); }

        /* Smart Cards Grid */
        .smart-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .smart-card {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            padding: 20px;
            border-radius: 20px;
            display: flex;
            align-items: center;
            gap: 15px;
            transition: 0.3s;
        }
        .smart-card:hover { border-color: var(--accent-purple); transform: translateY(-3px); }
        .icon-box {
            width: 50px; height: 50px;
            background: rgba(108, 92, 231, 0.1);
            border-radius: 12px;
            display: flex; align-items: center; justify-content: center;
            color: var(--accent-purple); font-size: 20px;
        }
        .card-info h3 { margin: 0; font-size: 24px; font-weight: 900; }
        .card-info p { margin: 0; font-size: 12px; color: var(--text-muted); }

        /* Search Box */
        .search-container {
            position: relative;
            margin-bottom: 25px;
        }
        .search-container input {
            width: 100%;
            height: 55px;
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 15px;
            padding: 0 50px 0 20px;
            color: white;
            outline: none;
        }
        .search-container i {
            position: absolute; right: 20px; top: 50%; transform: translateY(-50%);
            color: var(--text-muted);
        }

        /* Table Design */
        .table-wrapper {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
        }

        table { width: 100%; border-collapse: collapse; }
        th {
            background: rgba(255,255,255,0.03);
            color: var(--text-muted);
            padding: 20px;
            font-size: 13px;
            text-align: center;
            border-bottom: 1px solid var(--border-color);
        }

        td {
            padding: 16px;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.02);
            font-size: 14px;
        }

        .response-cell {
            text-align: right;
            color: #efefef;
            max-width: 400px;
            line-height: 1.6;
            background: rgba(255,255,255,0.02);
            border-radius: 10px;
            padding: 10px 15px !important;
        }

        /* Highlighting important columns */
        .id-text { font-family: monospace; color: var(--accent-purple); font-weight: bold; }
        .time-text { font-size: 12px; color: var(--text-muted); }

        @media (max-width: 768px) {
            .nav-header { flex-direction: column; gap: 15px; }
            .table-wrapper { overflow-x: auto; }
        }
    </style>
</head>
<body>

    <div class="nav-header">
        <h2>
            <i class="fa-solid fa-file-invoice"></i>
            مركز ردود الأوردات: <%= zone %>
        </h2>
        <a href="/dashboard" class="back-btn">
            <i class="fa-solid fa-house"></i>
            العودة للرئيسية
        </a>
    </div>

    <div class="smart-grid">
        <div class="smart-card">
            <div class="icon-box"><i class="fa-solid fa-list-check"></i></div>
            <div class="card-info">
                <h3><%= orders.length %></h3>
                <p>إجمالي الردود المستلمة</p>
            </div>
        </div>
        <div class="smart-card">
            <div class="icon-box" style="background: rgba(255, 94, 0, 0.1); color: var(--tanta-orange);">
                <i class="fa-solid fa-clock-rotate-left"></i>
            </div>
            <div class="card-info">
                <h3 id="todayCount">جاري الحصر...</h3>
                <p>ردود اليوم</p>
            </div>
        </div>
    </div>

    <div class="search-container">
        <i class="fa-solid fa-magnifying-glass"></i>
        <input type="text" id="searchInput" placeholder="بحث باسم المندوب، الكود، أو محتوى الرد..." onkeyup="filterTable()">
    </div>

    <div class="table-wrapper">
        <table id="ordersTable">
            <thead>
                <tr>
                    <% headers.forEach(h => { %> 
                        <th><%= h %></th> 
                    <% }) %>
                </tr>
            </thead>
            <tbody>
                <% orders.forEach(r => { %>
                <tr>
                    <% headers.forEach(h => { 
                        let val = r.get(h) || '---';
                        let isResponse = (h === 'الرد' || h === 'ملاحظات');
                        let isID = (h.toLowerCase().includes('id') || h.includes('كود'));
                    %>
                        <td class="<%= isResponse ? 'response-cell' : '' %> <%= isID ? 'id-text' : '' %>">
                            <%= val %>
                        </td>
                    <% }) %>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <script>
        function filterTable() {
            const input = document.getElementById('searchInput');
            const filter = input.value.toLowerCase();
            const rows = document.querySelectorAll('#ordersTable tbody tr');

            rows.forEach(row => {
                const text = row.textContent.toLowerCase();
                row.style.display = text.includes(filter) ? '' : 'none';
            });
        }

        // حساب ردود اليوم برمجياً (تبسيط)
        window.onload = function() {
            const rows = document.querySelectorAll('#ordersTable tbody tr');
            const today = new Date().toLocaleDateString('en-GB'); // بصيغة DD/MM/YYYY
            let count = 0;
            
            rows.forEach(row => {
                if(row.innerText.includes(today)) count++;
            });
            document.getElementById('todayCount').innerText = count;
        };
    </script>
</body>
</html>