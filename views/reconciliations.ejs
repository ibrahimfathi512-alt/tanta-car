<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reconciliations | Tanta Car Elite</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --tanta-orange: #ff5e00;
            --main-dark: #09090b;
            --card-bg: rgba(23, 23, 26, 0.8);
            --border-color: rgba(255, 255, 255, 0.08);
            --success: #00b894;
            --danger: #d63031;
            --text-muted: #a1a1aa;
        }

        body {
            background-color: var(--main-dark);
            background-image: radial-gradient(circle at 50% 0%, rgba(255, 94, 0, 0.07) 0%, transparent 50%);
            color: #fff;
            font-family: 'Cairo', sans-serif;
            margin: 0;
            padding: 25px;
            min-height: 100vh;
        }

        /* Header Area */
        .nav-header {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--border-color);
            padding: 20px 35px;
            border-radius: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .nav-header h2 { margin: 0; font-size: 22px; font-weight: 800; color: #fff; }
        .nav-header h2 i { color: var(--tanta-orange); margin-left: 10px; }

        .back-home {
            text-decoration: none;
            color: var(--tanta-orange);
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: 0.3s;
        }
        .back-home:hover { transform: translateX(5px); }

        /* Filter Controls */
        .controls-card {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            padding: 25px;
            border-radius: 24px;
            margin-bottom: 25px;
        }

        .controls-grid {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 20px;
        }

        .input-wrapper { position: relative; }
        .input-wrapper i {
            position: absolute;
            right: 18px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--tanta-orange);
        }

        input, select {
            width: 100%;
            height: 55px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid var(--border-color);
            border-radius: 14px;
            padding: 0 50px 0 20px;
            color: white;
            font-size: 15px;
            outline: none;
            transition: 0.3s;
        }

        input:focus, select:focus { border-color: var(--tanta-orange); background: rgba(255,255,255,0.06); }

        /* Table Design */
        .table-container {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
        }

        table { width: 100%; border-collapse: collapse; }
        th {
            background: rgba(255,255,255,0.03);
            color: var(--text-muted);
            padding: 20px;
            font-size: 13px;
            text-transform: uppercase;
            border-bottom: 1px solid var(--border-color);
        }

        td {
            padding: 16px;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.02);
            font-weight: 600;
        }

        /* Status Styling */
        .status-accepted {
            color: var(--success) !important;
            background: rgba(0, 184, 148, 0.1);
            padding: 6px 15px;
            border-radius: 10px;
            border: 1px solid rgba(0, 184, 148, 0.2);
        }

        .status-rejected {
            color: var(--danger) !important;
            background: rgba(214, 48, 49, 0.1);
            padding: 6px 15px;
            border-radius: 10px;
            border: 1px solid rgba(214, 48, 49, 0.2);
        }

        .hidden { display: none !important; }

        /* Counter Badge */
        .counter-badge {
            background: var(--tanta-orange);
            color: white;
            padding: 2px 10px;
            border-radius: 8px;
            font-size: 12px;
            margin-right: 10px;
        }

        @media (max-width: 768px) {
            .controls-grid { grid-template-columns: 1fr; }
            .nav-header { flex-direction: column; gap: 15px; text-align: center; }
        }
    </style>
</head>
<body>

    <div class="nav-header">
        <h2>
            <i class="fa-solid fa-handshake-angle"></i>
            سجل التصالحات الذكي
            <span class="counter-badge" id="rowCount"><%= data.length %> عملية</span>
        </h2>
        <a href="/dashboard" class="back-home">
            <i class="fa-solid fa-chevron-right"></i>
            العودة للرئيسية
        </a>
    </div>

    <div class="controls-card">
        <div class="controls-grid">
            <div class="input-wrapper">
                <i class="fa-solid fa-magnifying-glass"></i>
                <input type="text" id="searchInput" placeholder="بحث سريع بكود المندوب، الاسم، أو الحالة...">
            </div>
            <div class="input-wrapper">
                <i class="fa-solid fa-calendar-day"></i>
                <select id="dateSelect" onchange="applyFilters()">
                    <option value="">جميع الأيام</option>
                    <% 
                        let uniqueDates = [...new Set(data.map(row => row['التاريخ']))].filter(d => d);
                        uniqueDates.forEach(date => { 
                    %>
                        <option value="<%= date %>"><%= date %></option>
                    <% }) %>
                </select>
            </div>
        </div>
    </div>

    <div class="table-container">
        <table id="reconTable">
            <thead>
                <tr>
                    <% headers.forEach(h => { %> 
                        <th><%= h %></th> 
                    <% }) %>
                </tr>
            </thead>
            <tbody>
                <% data.forEach(row => { %>
                <tr class="data-row">
                    <% headers.forEach(h => { 
                        let val = row[h] || ''; 
                        let cellClass = "";
                        if(val === "قبول") cellClass = "status-accepted";
                        if(val === "رفض") cellClass = "status-rejected";
                        
                        // تحديد عمود التاريخ للفلترة لاحقاً
                        let isDateCol = (h === 'التاريخ');
                    %>
                    <td>
                        <span class="<%= cellClass %> <%= isDateCol ? 'cell-date' : '' %>">
                            <%= val %>
                        </span>
                    </td>
                    <% }) %>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <script>
        const searchInput = document.getElementById('searchInput');
        const dateSelect = document.getElementById('dateSelect');
        const rows = Array.from(document.getElementsByClassName('data-row'));
        const rowCountBadge = document.getElementById('rowCount');

        function applyFilters() {
            const searchTerm = searchInput.value.toLowerCase().trim();
            const selectedDate = dateSelect.value;
            let visibleCount = 0;

            rows.forEach(row => {
                const text = row.textContent.toLowerCase();
                const dateCell = row.querySelector('.cell-date') ? row.querySelector('.cell-date').textContent.trim() : "";

                const matchesSearch = searchTerm === "" || text.includes(searchTerm);
                const matchesDate = selectedDate === "" || dateCell === selectedDate;

                if (matchesSearch && matchesDate) {
                    row.classList.remove('hidden');
                    visibleCount++;
                } else {
                    row.classList.add('hidden');
                }
            });

            rowCountBadge.textContent = `${visibleCount} عملية`;
        }

        // تحسين البحث بإضافة Debounce بسيط
        let timeout = null;
        searchInput.addEventListener('input', () => {
            clearTimeout(timeout);
            timeout = setTimeout(applyFilters, 200);
        });
    </script>
</body>
</html>