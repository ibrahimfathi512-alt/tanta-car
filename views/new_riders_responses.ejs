<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recruitment Logs | Tanta Car Elite</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --tanta-orange: #ff5e00;
            --main-dark: #09090b;
            --card-bg: rgba(23, 23, 26, 0.85);
            --border-color: rgba(255, 255, 255, 0.08);
            --success: #22c55e;
            --text-muted: #a1a1aa;
            --accent-glow: rgba(255, 94, 0, 0.15);
        }

        body {
            background-color: var(--main-dark);
            background-image: 
                radial-gradient(circle at 0% 0%, rgba(108, 92, 231, 0.05) 0%, transparent 40%),
                radial-gradient(circle at 100% 100%, var(--accent-glow) 0%, transparent 40%);
            color: #fff;
            font-family: 'Cairo', sans-serif;
            margin: 0;
            padding: 30px;
            min-height: 100vh;
        }

        /* Nav Header */
        .nav-header {
            background: var(--card-bg);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid var(--border-color);
            padding: 20px 40px;
            border-radius: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 35px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.4);
        }

        .header-info { display: flex; align-items: center; gap: 20px; }
        .header-info i { color: var(--tanta-orange); font-size: 24px; }
        .header-info .title { font-weight: 900; font-size: 20px; letter-spacing: -0.5px; }
        .zone-tag { 
            background: rgba(255, 94, 0, 0.1); 
            color: var(--tanta-orange); 
            padding: 5px 15px; 
            border-radius: 12px; 
            font-size: 12px; 
            font-weight: 800;
            border: 1px solid rgba(255, 94, 0, 0.2);
        }

        .btn-back {
            text-decoration: none;
            background: var(--tanta-orange);
            color: white;
            padding: 10px 25px;
            border-radius: 15px;
            font-weight: 700;
            font-size: 14px;
            transition: 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .btn-back:hover {
            transform: scale(1.05);
            box-shadow: 0 0 20px rgba(255, 94, 0, 0.4);
        }

        /* Table Area */
        .table-container {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 32px;
            overflow: hidden;
            box-shadow: 0 25px 50px rgba(0,0,0,0.5);
        }

        table { width: 100%; border-collapse: collapse; }
        
        th {
            background: rgba(255, 255, 255, 0.03);
            color: var(--text-muted);
            padding: 22px;
            text-align: center;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-bottom: 1px solid var(--border-color);
        }

        td {
            padding: 20px;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.02);
            font-size: 14px;
            font-weight: 600;
            transition: 0.3s;
        }

        tr:hover td { background: rgba(255, 255, 255, 0.01); }

        /* Status Approved Styling */
        .status-approved-pill {
            background: rgba(34, 197, 94, 0.15);
            color: var(--success);
            padding: 8px 16px;
            border-radius: 14px;
            font-weight: 900;
            font-size: 12px;
            border: 1px solid rgba(34, 197, 94, 0.3);
            box-shadow: 0 0 15px rgba(34, 197, 94, 0.1);
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .status-approved-pill::before {
            content: '';
            width: 6px; height: 6px;
            background: var(--success);
            border-radius: 50%;
            box-shadow: 0 0 8px var(--success);
        }

        /* Search Bar Simulation */
        .search-wrapper {
            position: relative;
            margin-bottom: 25px;
            max-width: 400px;
        }
        .search-wrapper input {
            width: 100%;
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            padding: 15px 45px 15px 20px;
            border-radius: 18px;
            color: white;
            outline: none;
            font-family: 'Cairo';
        }
        .search-wrapper i {
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-muted);
        }

        /* Scrollbar */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: var(--main-dark); }
        ::-webkit-scrollbar-thumb { background: var(--border-color); border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: var(--tanta-orange); }

    </style>
</head>
<body>

    <div class="nav-header">
        <div class="header-info">
            <i class="fa-solid fa-clipboard-check"></i>
            <div>
                <div class="title">سجل ردود التعيينات</div>
                <div style="font-size: 10px; color: var(--text-muted);">نظام مراقبة التعيينات الفوري</div>
            </div>
            <span class="zone-tag">ZONE: <%= zone %></span>
        </div>
        <a href="/dashboard" class="btn-back">
            <i class="fa-solid fa-house-user"></i>
            العودة للرئيسية
        </a>
    </div>

    <div class="search-wrapper">
        <i class="fa-solid fa-magnifying-glass"></i>
        <input type="text" id="logSearch" placeholder="بحث سريع في الردود..." onkeyup="filterLogs()">
    </div>

    <div class="table-container">
        <table id="responsesTable">
            <thead>
                <tr>
                    <% headers.forEach(h => { %> <th><%= h %></th> <% }) %>
                </tr>
            </thead>
            <tbody>
                <% responses.forEach(r => { %>
                <tr>
                    <% headers.forEach(h => { 
                        let cellValue = r.get(h);
                        let isApproved = (cellValue === 'Approved & Created');
                    %>
                        <td>
                            <% if(isApproved) { %>
                                <span class="status-approved-pill">
                                    <%= cellValue %>
                                </span>
                            <% } else { %>
                                <span style="<%= h === 'الاسم' ? 'font-weight: 900; color: #fff;' : 'color: var(--text-muted);' %>">
                                    <%= cellValue %>
                                </span>
                            <% } %>
                        </td>
                    <% }) %>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <script>
        function filterLogs() {
            const input = document.getElementById('logSearch');
            const filter = input.value.toLowerCase();
            const table = document.getElementById('responsesTable');
            const tr = table.getElementsByTagName('tr');

            for (let i = 1; i < tr.length; i++) {
                const text = tr[i].textContent.toLowerCase();
                tr[i].style.display = text.includes(filter) ? "" : "none";
            }
        }
    </script>

    <div style="text-align: center; margin-top: 30px; font-size: 11px; color: var(--text-muted); letter-spacing: 2px;">
        TANTA CAR RECRUITMENT LOGS // END-TO-END ENCRYPTED // 2026
    </div>

</body>
</html>