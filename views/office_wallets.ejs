<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Office Wallets | Tanta Car Elite</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
    
    <style>
        :root {
            --accent-purple: #6c5ce7;
            --accent-hover: #a29bfe;
            --main-dark: #09090b;
            --card-bg: rgba(23, 23, 26, 0.95);
            --border-color: rgba(255, 255, 255, 0.08);
            --text-muted: #a1a1aa;
            --danger: #ef4444;
            --success: #22c55e;
            --input-focus-bg: rgba(108, 92, 231, 0.05);
        }

        body {
            background-color: var(--main-dark);
            background-image: radial-gradient(circle at 100% 0%, rgba(108, 92, 231, 0.07) 0%, transparent 50%);
            color: #fff;
            font-family: 'Cairo', sans-serif;
            margin: 0;
            padding: 25px;
            min-height: 100vh;
        }

        /* Nav Header */
        .nav-header {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--border-color);
            padding: 20px 35px;
            border-radius: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .header-actions { display: flex; gap: 15px; align-items: center; }

        .btn-export {
            background: var(--success);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 12px;
            cursor: pointer;
            font-family: 'Cairo';
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: 0.3s;
        }

        .btn-export:hover { transform: translateY(-2px); box-shadow: 0 5px 15px rgba(34, 197, 94, 0.3); }

        /* Stats Cards */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 20px;
            margin-bottom: 25px;
        }

        .stat-card {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            padding: 22px;
            border-radius: 20px;
            position: relative;
            transition: 0.3s;
        }

        .stat-card:hover { border-color: var(--accent-purple); transform: translateY(-5px); }
        .stat-card h4 { color: var(--text-muted); margin: 0; font-size: 13px; }
        .stat-card h2 { margin: 12px 0 0 0; font-size: 28px; font-weight: 900; color: #fff; }

        /* Controls */
        .controls-card {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            padding: 20px;
            border-radius: 20px;
            margin-bottom: 25px;
        }

        .grid-layout { display: grid; grid-template-columns: 2fr 1fr; gap: 20px; }
        .input-wrapper { position: relative; }
        .input-wrapper i { position: absolute; right: 18px; top: 50%; transform: translateY(-50%); color: var(--accent-purple); }

        input, select {
            width: 100%; height: 52px;
            background: #18181b;
            border: 1px solid var(--border-color);
            border-radius: 12px; padding: 0 45px 0 15px;
            color: #f4f4f5; outline: none; transition: 0.3s;
            font-family: 'Cairo';
        }

        input:focus { border-color: var(--accent-purple); box-shadow: 0 0 15px rgba(108, 92, 231, 0.1); }

        /* Table Design */
        .table-wrapper {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 24px;
            overflow: hidden;
        }

        table { width: 100%; border-collapse: collapse; }
        th { background: rgba(255,255,255,0.03); color: var(--text-muted); padding: 18px; font-size: 13px; }
        td { padding: 15px; text-align: center; border-bottom: 1px solid rgba(255,255,255,0.02); }
        
        .data-row { transition: 0.2s; }
        .data-row:hover { background: rgba(108, 92, 231, 0.03); }

        .wallet-amount { color: var(--danger); font-family: 'Courier New', monospace; font-weight: 900; }
        .id-badge { color: var(--accent-purple); background: rgba(108, 92, 231, 0.1); padding: 4px 10px; border-radius: 8px; font-size: 12px; }

        .hidden { display: none !important; }
        #noResults { padding: 50px; text-align: center; color: var(--text-muted); display: none; }

        @media (max-width: 768px) {
            .grid-layout { grid-template-columns: 1fr; }
            .nav-header { flex-direction: column; gap: 15px; }
        }
    </style>
</head>
<body>

    <div class="nav-header">
        <div>
            <h2><i class="fa-solid fa-wallet"></i> مديونيات المكتب - <%= zone %></h2>
        </div>
        <div class="header-actions">
            <button onclick="exportToExcel()" class="btn-export">
                <i class="fa-solid fa-file-excel"></i>
                تحميل Excel
            </button>
            <a href="/dashboard" class="back-btn" style="text-decoration: none; color: var(--accent-purple); font-weight: 700;">
                <i class="fa-solid fa-house"></i>
                الرئيسية
            </a>
        </div>
    </div>

    <div class="stats-grid">
        <div class="stat-card">
            <h4>إجمالي المديونية الحالية</h4>
            <h2 id="totalDebt">0</h2>
            <i class="fa-solid fa-money-bill-trend-up" style="position: absolute; left: 20px; bottom: 20px; opacity: 0.1; font-size: 40px; color: var(--accent-purple);"></i>
        </div>
        <div class="stat-card">
            <h4>عدد السجلات الظاهرة</h4>
            <h2 id="visibleCount">0</h2>
            <i class="fa-solid fa-users" style="position: absolute; left: 20px; bottom: 20px; opacity: 0.1; font-size: 40px; color: var(--accent-purple);"></i>
        </div>
        <div class="stat-card">
            <h4>آخر تحديث للكشف</h4>
            <h2 style="font-size: 18px;"><%= wallets.length > 0 ? wallets[0].Date : '---' %></h2>
            <i class="fa-solid fa-clock" style="position: absolute; left: 20px; bottom: 20px; opacity: 0.1; font-size: 40px; color: var(--accent-purple);"></i>
        </div>
    </div>

    <div class="controls-card">
        <div class="grid-layout">
            <div class="input-wrapper">
                <i class="fa-solid fa-magnifying-glass"></i>
                <input type="text" id="searchInput" placeholder="ابحث باسم المندوب، الكود، أو القيمة...">
            </div>
            <div class="input-wrapper">
                <i class="fa-solid fa-calendar-days"></i>
                <select id="dateSelect" onchange="applyFilters()">
                    <option value="">كل التواريخ</option>
                    <% let uniqueDates = [...new Set(wallets.map(w => w.Date))].filter(d => d && d !== '0');
                       uniqueDates.forEach(date => { %>
                        <option value="<%= date %>"><%= date %></option>
                    <% }) %>
                </select>
            </div>
        </div>
    </div>

    <div class="table-wrapper">
        <table id="walletTable">
            <thead>
                <tr>
                    <% headers.forEach(h => { %> 
                        <th><%= h %></th> 
                    <% }) %>
                </tr>
            </thead>
            <tbody id="tableBody">
                <% wallets.forEach(row => { %>
                <tr class="data-row">
                    <% headers.forEach(h => { 
                        let val = row[h] || '0';
                        let isWallet = (h === 'المحفظه' || h.includes('مديونية') || h.includes('قيمة'));
                        let isID = (h.includes('كود') || h === 'id');
                    %>
                    <td class="cell-<%= h %> <%= isWallet ? 'wallet-amount' : '' %>">
                        <% if(isID) { %>
                            <span class="id-badge"><%= val %></span>
                        <% } else { %>
                            <%= val %>
                        <% } %>
                    </td>
                    <% }) %>
                </tr>
                <% }) %>
            </tbody>
        </table>
        
        <div id="noResults">
            <i class="fa-solid fa-magnifying-glass-blur" style="font-size: 40px; opacity: 0.2; display: block; margin-bottom: 10px;"></i>
            عذراً، لم نجد نتائج تطابق بحثك
        </div>
    </div>

    <script>
        const searchInput = document.getElementById('searchInput');
        const dateSelect = document.getElementById('dateSelect');
        const rows = Array.from(document.querySelectorAll('.data-row'));
        const noResults = document.getElementById('noResults');
        const totalDebtEl = document.getElementById('totalDebt');
        const visibleCountEl = document.getElementById('visibleCount');

        function applyFilters() {
            const searchTerm = searchInput.value.toLowerCase();
            const selectedDate = dateSelect.value;
            let currentTotal = 0;
            let count = 0;

            rows.forEach(row => {
                const text = row.textContent.toLowerCase();
                const dateCell = row.querySelector('.cell-Date')?.textContent.trim() || "";
                
                const matchesSearch = text.includes(searchTerm);
                const matchesDate = !selectedDate || dateCell === selectedDate;

                if (matchesSearch && matchesDate) {
                    row.classList.remove('hidden');
                    // حساب المديونية للصفوف الظاهرة فقط
                    const amountCell = row.querySelector('.wallet-amount');
                    if(amountCell) {
                        currentTotal += parseFloat(amountCell.textContent.replace(/,/g, '')) || 0;
                    }
                    count++;
                } else {
                    row.classList.add('hidden');
                }
            });

            // تحديث الإحصائيات
            totalDebtEl.innerText = currentTotal.toLocaleString('ar-EG') + ' ج.م';
            visibleCountEl.innerText = count;
            noResults.style.display = count === 0 ? "block" : "none";
        }

        // دالة التصدير للإكسيل
        function exportToExcel() {
            const zoneName = "<%= zone %>";
            const date = new Date().toLocaleDateString('ar-EG').replace(/\//g, '-');
            
            // تحويل الجدول إلى ورقة عمل، مع استثناء الصفوف المخفية
            const table = document.getElementById('walletTable');
            const ws = XLSX.utils.table_to_sheet(table, { display: false });
            const wb = XLSX.utils.book_new();
            XLSX.utils.book_append_sheet(wb, ws, "مديونيات");
            
            // تحميل الملف
            XLSX.writeFile(wb, `مديونية_${zoneName}_${date}.xlsx`);
        }

        // Debounce للبحث لتحسين الأداء
        let timeout = null;
        searchInput.addEventListener('input', () => {
            clearTimeout(timeout);
            timeout = setTimeout(applyFilters, 200);
        });

        // التشغيل المبدئي
        window.onload = applyFilters;
    </script>
</body>
</html>