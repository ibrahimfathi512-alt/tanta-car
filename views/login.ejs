<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tanta Car Fleet | Control Center</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --tanta-orange: #ff5e00;
            --main-dark: #07080a;
            /* زيادة شفافية الكارت للسماح بتداخل الصورة بشكل أكبر */
            --glass: rgba(0, 0, 0, 0.45); 
            --glass-border: rgba(255, 255, 255, 0.12);
        }

        * {
            margin: 0; padding: 0; box-sizing: border-box;
            font-family: 'Cairo', sans-serif;
        }

        body {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: var(--main-dark);
            overflow: hidden;
            position: relative;
        }

        /* الخلفية: دمج الصورة مع طبقة لونية خفيفة جداً للحفاظ على وضوحها */
        .hero-background {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(rgba(0,0,0,0.2), rgba(0,0,0,0.6)), 
                        url('/images/background.jpg'); 
            background-size: cover;
            background-position: center;
            z-index: 1;
            /* الحفاظ على تفاصيل الصورة الجديدة */
            filter: contrast(1.05) brightness(0.9);
        }

        /* كارت تسجيل الدخول: التداخل (Glassmorphism) */
        .login-card {
            position: relative;
            z-index: 10;
            width: 90%;
            max-width: 420px;
            background: var(--glass);
            /* أهم سطر للتداخل: تغبيش الخلفية خلف الزجاج */
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            border-radius: 28px;
            padding: 45px 35px;
            box-shadow: 0 25px 50px rgba(0,0,0,0.5);
            animation: fadeInScale 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        }

        @keyframes fadeInScale {
            from { opacity: 0; transform: scale(0.95) translateY(20px); }
            to { opacity: 1; transform: scale(1) translateY(0); }
        }

        .header-section {
            text-align: center;
            margin-bottom: 35px;
        }

        .header-section h1 {
            font-size: 28px;
            font-weight: 900;
            color: #fff;
            text-shadow: 0 2px 10px rgba(0,0,0,0.3);
            margin-top: 10px;
        }

        .header-section span {
            color: var(--tanta-orange);
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 5px;
            font-weight: 900;
            display: block;
        }

        .input-group {
            margin-bottom: 22px;
        }

        .input-group label {
            display: block;
            color: rgba(255,255,255,0.7);
            font-size: 13px;
            margin-bottom: 8px;
            margin-right: 5px;
        }

        .field-wrapper {
            position: relative;
        }

        .field-wrapper i {
            position: absolute;
            right: 18px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--tanta-orange);
            font-size: 18px;
            text-shadow: 0 0 10px rgba(255, 94, 0, 0.3);
        }

        select, input {
            width: 100%;
            height: 56px;
            background: rgba(255, 255, 255, 0.07);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 14px;
            padding: 0 50px 0 20px;
            color: #fff;
            font-size: 16px;
            outline: none;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        select:focus, input:focus {
            border-color: var(--tanta-orange);
            background: rgba(255, 255, 255, 0.12);
            box-shadow: 0 0 15px rgba(255, 94, 0, 0.3);
        }

        .login-btn {
            width: 100%;
            height: 60px;
            background: linear-gradient(135deg, var(--tanta-orange), #ff8c00);
            color: #fff;
            border: none;
            border-radius: 14px;
            font-size: 18px;
            font-weight: 900;
            cursor: pointer;
            margin-top: 15px;
            transition: all 0.4s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            box-shadow: 0 10px 25px rgba(255, 94, 0, 0.4);
        }

        .login-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(255, 94, 0, 0.5);
            filter: brightness(1.1);
        }

        .footer {
            margin-top: 30px;
            text-align: center;
            font-size: 11px;
            color: rgba(255, 255, 255, 0.4);
            letter-spacing: 2px;
        }

        /* تحسين مظهر القائمة المنسدلة */
        select option { background: #1a1a1a; color: #fff; }
        select { appearance: none; -webkit-appearance: none; }
    </style>
</head>
<body>

    <div class="hero-background"></div>

    <div class="login-card">
        <div class="header-section">
            <span>TANTA CAR HUB</span>
            <h1>دخول النظام</h1>
        </div>

        <form action="/login" method="POST">
            <div class="input-group">
                <label>منطقة العمل (الزون)</label>
                <div class="field-wrapper">
                    <i class="fas fa-satellite-dish"></i>
                    <select name="zone" required>
                        <option value="" disabled selected>تحديد المنطقة...</option>
                        <% if (typeof zones !== 'undefined') { %>
                            <% zones.forEach(function(zone) { %>
                                <option value="<%= zone %>"><%= zone %></option>
                            <% }); %>
                        <% } %>
                    </select>
                </div>
            </div>

            <div class="input-group">
                <label>رمز الوصول الآمن</label>
                <div class="field-wrapper">
                    <i class="fas fa-shield-alt"></i>
                    <input type="password" name="password" placeholder="Key Code" required>
                </div>
            </div>

            <button type="submit" class="login-btn">
                <span>فتح المنصة</span>
                <i class="fas fa-unlock-alt"></i>
            </button>
        </form>

        <div class="footer">
            2026 // FLEET MANAGEMENT GATE
        </div>
    </div>

</body>
</html>